#!/bin/bash

# Using system Python with PyQt6 installed
# We needed to install the libxcb-cursor0 package for the Qt xcb platform plugin

# Run the script with system Python
python3 "$(dirname "$0")"/video_splitter/video_splitter.py 